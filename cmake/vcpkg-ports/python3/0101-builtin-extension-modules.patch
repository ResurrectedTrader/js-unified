diff --git a/Modules/_ctypes/callbacks.c b/Modules/_ctypes/callbacks.c
index ec113e4..d013954 100644
--- a/Modules/_ctypes/callbacks.c
+++ b/Modules/_ctypes/callbacks.c
@@ -533,6 +533,9 @@ long Call_GetClassObject(REFCLSID rclsid, REFIID riid, LPVOID *ppv)
     return E_FAIL;
 }
 
+/* vcpkg: a _ctypes built into a static core is part of someone else's module, which
+   must not acquire COM server exports it never asked for (and cannot define its own). */
+#ifndef Py_BUILD_CORE_BUILTIN
 STDAPI DllGetClassObject(REFCLSID rclsid,
                          REFIID riid,
                          LPVOID *ppv)
@@ -546,6 +549,7 @@ STDAPI DllGetClassObject(REFCLSID rclsid,
     PyGILState_Release(state);
     return result;
 }
+#endif /* !Py_BUILD_CORE_BUILTIN */
 
 long Call_CanUnloadNow(void)
 {
@@ -579,6 +583,7 @@ long Call_CanUnloadNow(void)
   DllRegisterServer and DllUnregisterServer still missing
 */
 
+#ifndef Py_BUILD_CORE_BUILTIN
 STDAPI DllCanUnloadNow(void)
 {
     long result;
@@ -587,6 +592,7 @@ STDAPI DllCanUnloadNow(void)
     PyGILState_Release(state);
     return result;
 }
+#endif /* !Py_BUILD_CORE_BUILTIN */
 
 #ifndef Py_NO_ENABLE_SHARED
 BOOL WINAPI DllMain(HINSTANCE hinstDLL, DWORD fdwReason, LPVOID lpvRes)
diff --git a/PC/_wmimodule.cpp b/PC/_wmimodule.cpp
index b9a229b..47ce864 100644
--- a/PC/_wmimodule.cpp
+++ b/PC/_wmimodule.cpp
@@ -117,7 +117,7 @@ _query_thread(LPVOID param)
     }
     if (SUCCEEDED(hr)) {
         hr = services->ExecQuery(
-            bstr_t("WQL"),
+            bstr_t(L"WQL"),  // vcpkg: wide - a narrow one needs comsupp.lib's ConvertStringToBSTR
             bstrQuery,
             WBEM_FLAG_FORWARD_ONLY | WBEM_FLAG_RETURN_IMMEDIATELY,
             NULL,
diff --git a/PC/config.c b/PC/config.c
index 6ce2131..93b66cb 100644
--- a/PC/config.c
+++ b/PC/config.c
@@ -88,6 +88,9 @@ extern PyObject* PyInit__suggestions(void);
 /* tools/freeze/makeconfig.py marker for additional "extern" */
 /* -- ADDMODULE MARKER 1 -- */
 
+/* vcpkg: extension modules compiled into a static core (generated by the port) */
+#include "vcpkg_builtin_modules.h"
+
 extern PyObject* PyMarshal_Init(void);
 extern PyObject* PyInit__imp(void);
 
@@ -172,6 +175,8 @@ struct _inittab _PyImport_Inittab[] = {
 /* tools/freeze/makeconfig.py marker for additional "_inittab" entries */
 /* -- ADDMODULE MARKER 2 -- */
 
+    VCPKG_BUILTIN_INITTAB
+
     /* This module "lives in" with marshal.c */
     {"marshal", PyMarshal_Init},
 
